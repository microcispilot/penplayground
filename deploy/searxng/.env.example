# Copy to .env (standalone use). In the pen-playground stack the same variable lives in
# /srv/pen-playground/.env instead (see deploy/deploy.sh).
# Generate with: openssl rand -hex 32
SEARXNG_SECRET=
